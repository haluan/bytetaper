routes:
  - id: "api-v1-proxy"
    match:
      kind: "prefix"
      prefix: "/api/v1/"
    method: "get"
    mutation: "disabled"
    field_filter:
      mode: "allowlist"
      fields:
        - "id"
        - "name"
        - "status"

  - id: "admin-exact"
    match:
      kind: "exact"
      prefix: "/admin"
    mutation: "headers_only"
