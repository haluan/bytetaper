# ByteTaper Performance Regression Guardrails Configuration
# Thresholds are baseline-focused safety limits, not marketing claims.

bytetaper_observe:
  max_p95_overhead_ms: 160.0
  max_error_rate: 0.05
  min_payload_reduction_ratio: 0.00

bytetaper_field_filtering:
  max_p95_overhead_ms: 180.0
  max_error_rate: 0.05
  min_payload_reduction_ratio: 0.00

cache_hit:
  max_p95_overhead_ms: 120.0
  max_error_rate: 0.05
  min_payload_reduction_ratio: 0.00

pagination_guardrail:
  max_p95_overhead_ms: 160.0
  max_error_rate: 0.05
  min_payload_reduction_ratio: 0.00

compression_coordination:
  max_p95_overhead_ms: 220.0
  max_error_rate: 0.05
  min_payload_reduction_ratio: 0.00

coalescing_burst:
  max_p95_overhead_ms: 150.0
  max_error_rate: 0.05
  min_payload_reduction_ratio: 0.00

envoy_only:
  max_p95_overhead_ms: 100.0
  max_error_rate: 0.05
  min_payload_reduction_ratio: 0.00

performance_smoke:
  max_p95_overhead_ms: 160.0
  max_error_rate: 0.05
  min_payload_reduction_ratio: 0.00

